openai>=1.40.0
httpx>=0.27.0
pydantic>=2.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
# uvicorn 0.51+ 的 ws_sansio_impl 需要 websockets>=13.0，只设下限即可。
# 警告：pyppeteer/undetected-chromedriver 等包会无约束拉旧版 websockets，
# 切勿在容器内安装这些包，否则会降级 websockets 到 <13 导致 uvicorn 崩溃。
websockets>=13.0
sqlalchemy>=2.0.0
greenlet>=3.0.0
aiosqlite>=0.19.0
python-multipart>=0.0.9
